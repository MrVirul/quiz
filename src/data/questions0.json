{
  "description": "Database Programming - Lecture 1 - Mid Practice Quiz",
  "questions": [
    {
      "id": 1,
      "question": "A developer opens an explicit cursor defined by the query: SELECT emp_name, salary FROM employees WHERE salary > 4000;. After the cursor is opened, but before any rows are fetched, a concurrent database session inserts 10 new employee records matching the query criteria and issues a COMMIT;. In the original session, the developer proceeds to fetch all records in a loop. What will the developer's cursor loop process?",
      "answers": [
        {
          "option": "A. The cursor will process only the records that existed and matched the criteria at the exact moment the cursor was opened.",
          "is_correct": true
        },
        {
          "option": "B. The cursor will dynamically detect the 10 new rows during FETCH because the cursor reads from the underlying active database.",
          "is_correct": false
        },
        {
          "option": "C. The cursor will raise a CURSOR_ALREADY_OPEN or TRANSACTION_SERIALIZATION_FAILURE exception due to concurrent modification.",
          "is_correct": false
        },
        {
          "option": "D. The cursor will fetch the new 10 rows only if the cursor is declared as a \"dynamic cursor\" in the PL/SQL declaration block.",
          "is_correct": false
        }
      ],
      "explanation": "Correct answer is A. When an explicit cursor is opened, the active set is established at the time of the OPEN statement based on read-consistency rules, so commits by other sessions after opening are not visible to the cursor's FETCH operations."
    },
    {
      "id": 2,
      "question": "A student writes the following PL/SQL block to fetch a department name: DECLARE v_dept_name VARCHAR2(50); BEGIN SELECT dept_name INTO v_dept_name FROM departments WHERE dept_id = 99; DBMS_OUTPUT.PUT_LINE('Department is: ' || v_dept_name); END; If dept_id = 99 matches exactly zero records in the departments table, what will happen during the execution of this block, and how does the database handle the cursor?",
      "answers": [
        {
          "option": "A. An implicit cursor is created, the block executes successfully, and v_dept_name remains uninitialized (NULL).",
          "is_correct": false
        },
        {
          "option": "B. An implicit cursor is created, and the database automatically raises the predefined exception NO_DATA_FOUND at runtime, causing the block to terminate abruptly.",
          "is_correct": true
        },
        {
          "option": "C. The database compiler rejects the block during the compilation phase because a single-row SELECT must always guarantee a returned row.",
          "is_correct": false
        },
        {
          "option": "D. An explicit cursor must be used instead; implicit cursors cannot handle SELECT queries that return zero rows under any circumstances.",
          "is_correct": false
        }
      ],
      "explanation": "Correct answer is B. When a single-row SELECT INTO returns zero rows, Oracle PL/SQL raises the predefined NO_DATA_FOUND exception at runtime, causing the block to terminate abruptly if the exception is unhandled."
    },
    {
      "id": 3,
      "question": "An explicit cursor contains exactly 3 rows in its active set. The developer writes a loop to fetch from this cursor 5 times sequentially without checking %NOTFOUND inside the loop. What is the behavior of the variables populated by the fetch on the 4th and 5th iterations?",
      "answers": [
        {
          "option": "A. The variables will be automatically set to NULL on the 4th and 5th fetches.",
          "is_correct": false
        },
        {
          "option": "B. The database raises an INVALID_CURSOR exception on the 4th fetch because the active set has been exhausted.",
          "is_correct": false
        },
        {
          "option": "C. The variables will retain the values fetched from the 3rd row during the 4th and 5th fetches.",
          "is_correct": true
        },
        {
          "option": "D. The loop terminates automatically at the 4th fetch because PL/SQL's execution engine performs implicit boundary checks.",
          "is_correct": false
        }
      ],
      "explanation": "Correct answer is C. When a FETCH fails to retrieve a new row, the target variables are not modified, so they retain the values from the last successfully fetched row."
    },
    {
      "id": 4,
      "question": "A table employees contains 5 records. A developer executes the following statement block: BEGIN UPDATE employees SET salary = salary * 1.1 WHERE department_id = 999; -- Line X END; Assuming department_id = 999 matches zero rows, what will the values of the implicit cursor attributes SQL%FOUND and SQL%ROWCOUNT be if evaluated at Line X?",
      "answers": [
        {
          "option": "A. SQL%FOUND is TRUE, and SQL%ROWCOUNT is 0.",
          "is_correct": false
        },
        {
          "option": "B. SQL%FOUND is FALSE, and SQL%ROWCOUNT is NULL.",
          "is_correct": false
        },
        {
          "option": "C. SQL%FOUND is FALSE, and SQL%ROWCOUNT is 0.",
          "is_correct": true
        },
        {
          "option": "D. An exception is thrown, preventing any attributes from being evaluated.",
          "is_correct": false
        }
      ],
      "explanation": "Correct answer is C. An UPDATE that affects zero rows raises no exception; SQL%FOUND is FALSE and SQL%ROWCOUNT is 0."
    },
    {
      "id": 5,
      "question": "What occurs if a standard SELECT query that returns exactly three rows is executed directly in the execution section of a PL/SQL block without an explicit cursor declaration, such as: BEGIN SELECT salary INTO v_sal FROM employees; END?",
      "answers": [
        {
          "option": "A. The block compiles successfully, and v_sal is populated with the value of the first record in the active set.",
          "is_correct": false
        },
        {
          "option": "B. The block compiles successfully, but at runtime, the database raises the TOO_MANY_ROWS predefined exception, and the block terminates abruptly if unhandled.",
          "is_correct": true
        },
        {
          "option": "C. The block fails compilation because standard SELECT statements in the execution section of PL/SQL must always have an explicit cursor.",
          "is_correct": false
        },
        {
          "option": "D. The block compiles and executes, but only the last retrieved row is stored in v_sal.",
          "is_correct": false
        }
      ],
      "explanation": "Correct answer is B. A SELECT INTO is designed to handle exactly one row; if the query returns more than one row, the predefined TOO_MANY_ROWS exception is raised at runtime."
    },
    {
      "id": 6,
      "question": "Which of the following statements correctly describes the properties of an explicit cursor? [Select ALL correct answers]",
      "answers": [
        {
          "option": "A. It is a temporary work area used to store data retrieved from the database and manipulate it.",
          "is_correct": true
        },
        {
          "option": "B. It can hold more than one row but can process only one row at a time.",
          "is_correct": true
        },
        {
          "option": "C. The set of rows the explicit cursor holds is called the active set.",
          "is_correct": true
        },
        {
          "option": "D. It is created automatically whenever an INSERT or UPDATE statement is executed.",
          "is_correct": false
        }
      ],
      "explanation": "Correct answers are A, B and C. A cursor is a temporary work area for retrieved data, can hold multiple rows but processes one at a time, and its rows form the active set. D is wrong because explicit cursors are user-defined, while implicit cursors are created automatically by DML."
    },
    {
      "id": 7,
      "question": "A student needs to choose between an explicit cursor and an implicit cursor. Which scenario strictly mandates the declaration and use of an explicit cursor?",
      "answers": [
        {
          "option": "A. Running an INSERT statement that adds a single record to the database.",
          "is_correct": false
        },
        {
          "option": "B. Executing a SELECT statement that is expected to return exactly one row for a specific primary key.",
          "is_correct": false
        },
        {
          "option": "C. Processing a multi-row query result-set sequentially, row-by-row, within a loop in PL/SQL.",
          "is_correct": true
        },
        {
          "option": "D. Deleting multiple records from a table using a DELETE statement.",
          "is_correct": false
        }
      ],
      "explanation": "Correct answer is C. Explicit cursors are required when processing a SELECT result that returns more than one row row-by-row; DML and single-row SELECTs are handled by implicit cursors."
    },
    {
      "id": 8,
      "question": "When processing an explicit cursor, how does the current row pointer behave, and how is it advanced?",
      "answers": [
        {
          "option": "A. The pointer is positioned at the first row when the cursor is declared, and advances only when the transaction commits.",
          "is_correct": false
        },
        {
          "option": "B. The pointer is uninitialized when the cursor is opened, and each FETCH operation retrieves the current row and advances the pointer to the next row in the active set.",
          "is_correct": true
        },
        {
          "option": "C. The pointer is managed by the client application and must be manually incremented using an index variable.",
          "is_correct": false
        },
        {
          "option": "D. The pointer is always fixed at the last row retrieved, requiring a PREVIOUS keyword to traverse backward.",
          "is_correct": false
        }
      ],
      "explanation": "Correct answer is B. Each FETCH retrieves the current row and automatically advances the pointer to the next row; cursors are forward-only traversal mechanisms."
    },
    {
      "id": 9,
      "question": "Under which of the following conditions does Oracle database create an implicit cursor by default? [Select ALL correct answers]",
      "answers": [
        {
          "option": "A. During the execution of an UPDATE statement that modifies 50 rows.",
          "is_correct": true
        },
        {
          "option": "B. During the execution of a DELETE statement that removes 0 rows.",
          "is_correct": true
        },
        {
          "option": "C. During the execution of a SELECT statement that returns exactly one row.",
          "is_correct": true
        },
        {
          "option": "D. During the execution of a DECLARE block containing local variables.",
          "is_correct": false
        }
      ],
      "explanation": "Correct answers are A, B and C. Implicit cursors are created for DML statements (INSERT/UPDATE/DELETE) regardless of how many rows are affected, and for single-row SELECTs. Declaring variables does not create a cursor."
    },
    {
      "id": 10,
      "question": "Why are explicit cursors preferred over implicit cursors when retrieving and processing a large dataset of 10,000 rows?",
      "answers": [
        {
          "option": "A. Implicit cursors will automatically load all 10,000 rows into the local client memory, causing an out-of-memory error.",
          "is_correct": false
        },
        {
          "option": "B. An implicit cursor cannot handle multiple rows and would raise a TOO_MANY_ROWS exception immediately upon finding more than one row during a standard SELECT INTO.",
          "is_correct": true
        },
        {
          "option": "C. Explicit cursors allow the database to bypass transaction logging, making row retrieval significantly faster.",
          "is_correct": false
        },
        {
          "option": "D. Explicit cursors are compiled faster because they do not require any validation checks.",
          "is_correct": false
        }
      ],
      "explanation": "Correct answer is B. A standard single-row SELECT INTO is structurally restricted to single-row results, so explicit cursors must be used to process a large dataset row-by-row."
    },
    {
      "id": 11,
      "question": "As a senior DBA, you are reviewing subprogram designs. Which of the following statements represents a correct structural difference between a Procedure and a Function in PL/SQL? [Select ALL correct answers]",
      "answers": [
        {
          "option": "A. Functions can accept multiple input parameters and return exactly one value, whereas Procedures can return multiple values via OUT parameters.",
          "is_correct": true
        },
        {
          "option": "B. Functions can be used inside standard SQL syntax (e.g., SELECT or WHERE clauses), whereas Procedures cannot.",
          "is_correct": true
        },
        {
          "option": "C. Procedures are mandatory for database operations, whereas Functions are optional utility blocks.",
          "is_correct": false
        },
        {
          "option": "D. A Function can return multiple values through OUT parameters in addition to its return value, but this prevents it from being called within standard SQL queries.",
          "is_correct": true
        }
      ],
      "explanation": "Correct answers are A, B and D. Procedures return multiple values via OUT parameters while functions return one value via RETURN, functions can be used in SQL syntax, and functions with OUT parameters cannot be called within SQL. C is wrong because both are modularization tools."
    },
    {
      "id": 12,
      "question": "Consider the following procedure definition: CREATE OR REPLACE PROCEDURE calculate_data (p_val1 IN NUMBER, p_val2 OUT NUMBER, p_val3 IN OUT NUMBER) IS BEGIN p_val2 := p_val1 * 2; p_val3 := p_val3 + p_val2; END calculate_data; If a developer executes this procedure with the following block: DECLARE v_a NUMBER := 10; v_b NUMBER := 50; v_c NUMBER := 5; BEGIN calculate_data(v_a, v_b, v_c); DBMS_OUTPUT.PUT_LINE(v_b || ' and ' || v_c); END; What will be the printed output of the block?",
      "answers": [
        {
          "option": "A. 20 and 25",
          "is_correct": true
        },
        {
          "option": "B. 20 and 75",
          "is_correct": false
        },
        {
          "option": "C. 100 and 105",
          "is_correct": false
        },
        {
          "option": "D. 50 and 25",
          "is_correct": false
        }
      ],
      "explanation": "Correct answer is A. p_val1 is 10, p_val2 (OUT) is set to 20, and p_val3 (IN OUT) starts at 5 and becomes 5 + 20 = 25, so v_b and v_c are updated to 20 and 25."
    },
    {
      "id": 13,
      "question": "A developer writes a procedure but gets a compilation error. What is the syntax error in the following block? CREATE OR REPLACE PROCEDURE display_message (p_msg IN VARCHAR2) DECLARE v_suffix VARCHAR2(10) := '!!!'; BEGIN DBMS_OUTPUT.PUT_LINE(p_msg || v_suffix); END display_message;",
      "answers": [
        {
          "option": "A. The parameter p_msg must specify a size constraint (e.g., VARCHAR2(30)).",
          "is_correct": false
        },
        {
          "option": "B. The keyword DECLARE is used. Procedures must use IS or AS and declare local variables before BEGIN without the DECLARE keyword.",
          "is_correct": true
        },
        {
          "option": "C. Procedures cannot accept parameters of type VARCHAR2 without a default value.",
          "is_correct": false
        },
        {
          "option": "D. The procedure lacks an EXCEPTION block, which is mandatory for all procedures.",
          "is_correct": false
        }
      ],
      "explanation": "Correct answer is B. In subprogram definitions the declaration section starts with the keyword IS or AS; DECLARE is only used for anonymous PL/SQL blocks. Note also that parameter declarations must not include size constraints."
    },
    {
      "id": 14,
      "question": "A student creates a function to calculate a discount: CREATE OR REPLACE FUNCTION apply_discount(p_price IN NUMBER) RETURN NUMBER IS BEGIN UPDATE inventory SET last_accessed = SYSDATE; RETURN p_price * 0.9; END apply_discount; When this function is executed via: SELECT apply_discount(price) FROM products;, what will happen?",
      "answers": [
        {
          "option": "A. The query executes successfully and updates the inventory table for every row in products.",
          "is_correct": false
        },
        {
          "option": "B. The query fails at runtime because functions called from SQL SELECT queries are not allowed to perform DML operations (such as UPDATE) on database tables.",
          "is_correct": true
        },
        {
          "option": "C. The query compiles but only updates the first row in inventory due to performance optimizations.",
          "is_correct": false
        },
        {
          "option": "D. The SQL engine automatically wraps the DML in an autonomous transaction and commits it.",
          "is_correct": false
        }
      ],
      "explanation": "Correct answer is B. A PL/SQL function used inside standard SQL cannot perform DML statements on database tables; attempting to do so raises a runtime exception."
    },
    {
      "id": 15,
      "question": "Which of the following represents a valid syntax for declaring parameters in a PL/SQL procedure or function?",
      "answers": [
        {
          "option": "A. CREATE PROCEDURE proc1 (p_id IN NUMBER(10))",
          "is_correct": false
        },
        {
          "option": "B. CREATE PROCEDURE proc1 (p_id IN NUMBER)",
          "is_correct": true
        },
        {
          "option": "C. CREATE PROCEDURE proc1 (p_name IN OUT VARCHAR2(50))",
          "is_correct": false
        },
        {
          "option": "D. CREATE PROCEDURE proc1 (p_date DATE(7))",
          "is_correct": false
        }
      ],
      "explanation": "Correct answer is B. In PL/SQL subprogram parameters only the base datatype name is specified (e.g., NUMBER, VARCHAR2, DATE); size or precision constraints are illegal in the parameter list."
    },
    {
      "id": 16,
      "question": "Based on the class activity to create update_salary_by_dept(p_dept_id IN NUMBER, p_percent IN NUMBER, p_updated_count OUT NUMBER): if a department ID does not exist in the database, what is the correct implementation behavior?",
      "answers": [
        {
          "option": "A. The UPDATE statement throws an implicit NO_DATA_FOUND exception, forcing execution to the exception block.",
          "is_correct": false
        },
        {
          "option": "B. The UPDATE statement completes without error, setting p_updated_count to 0, and the procedure completes gracefully.",
          "is_correct": true
        },
        {
          "option": "C. The database raises a TOO_MANY_ROWS exception because multiple updates were attempted.",
          "is_correct": false
        },
        {
          "option": "D. The database automatically rolls back the entire transaction history since the last connection.",
          "is_correct": false
        }
      ],
      "explanation": "Correct answer is B. SQL DML statements like UPDATE do not throw NO_DATA_FOUND when they match zero rows; the update affects 0 rows, so SQL%ROWCOUNT (0) should be assigned to p_updated_count."
    },
    {
      "id": 17,
      "question": "An examiner writes a function with an exception handler: CREATE OR REPLACE FUNCTION get_val(p_num IN NUMBER) RETURN VARCHAR2 IS BEGIN IF p_num > 100 THEN RETURN 'High'; END IF; EXCEPTION WHEN OTHERS THEN RETURN 'Error'; END get_val; If this function is called with p_num = 50, what happens at runtime?",
      "answers": [
        {
          "option": "A. The function returns NULL because the IF condition failed and no default was specified.",
          "is_correct": false
        },
        {
          "option": "B. The database raises an \"ORA-06503: PL/SQL: Function returned without value\" exception at runtime.",
          "is_correct": true
        },
        {
          "option": "C. The function automatically jumps to the EXCEPTION block and returns 'Error'.",
          "is_correct": false
        },
        {
          "option": "D. The function is rejected by the compiler during compilation because all execution paths must statically prove a return value.",
          "is_correct": false
        }
      ],
      "explanation": "Correct answer is B. With p_num = 50 the IF block is skipped and no RETURN executes; since no exception occurred, control reaches the END block and raises ORA-06503 (Function returned without value)."
    },
    {
      "id": 18,
      "question": "In the activity get_employee_annual_salary, the formula for the annual salary is defined as (salary * 12). However, if the employee has a bonus percentage, it must be added. If a developer implements the assignment as: v_annual := (v_salary * 12) + (v_salary * 12 * v_bonus_percent); What will happen if an employee has a valid v_salary but their v_bonus_percent is NULL in the database?",
      "answers": [
        {
          "option": "A. The function will calculate the salary ignoring the bonus (equivalent to a bonus of 0).",
          "is_correct": false
        },
        {
          "option": "B. The calculation will return NULL for the entire annual salary because any arithmetic operation involving a NULL operand yields NULL.",
          "is_correct": true
        },
        {
          "option": "C. The database automatically substitutes NULL with 1 during arithmetic operations.",
          "is_correct": false
        },
        {
          "option": "D. A VALUE_ERROR exception is thrown at runtime, interrupting the function.",
          "is_correct": false
        }
      ],
      "explanation": "Correct answer is B. Any arithmetic operation involving a NULL operand yields NULL, so the whole annual salary becomes NULL. NVL or COALESCE must be used to handle NULLs."
    },
    {
      "id": 19,
      "question": "Review the database procedure from the slides: CREATE OR REPLACE PROCEDURE update_emp_salary (p_emp_id IN NUMBER, p_percent IN NUMBER) IS BEGIN UPDATE employees SET salary = salary + (salary * (p_percent / 100)) WHERE employee_id = p_emp_id; COMMIT; DBMS_OUTPUT.PUT_LINE('Salary updated.'); END update_emp_salary; If a calling anonymous block performs several inserts into another audit table, and then calls update_emp_salary, what is the scope of the COMMIT; statement executed inside the procedure?",
      "answers": [
        {
          "option": "A. The COMMIT only saves the UPDATE statement inside the procedure, leaving the inserts in the calling block uncommitted.",
          "is_correct": false
        },
        {
          "option": "B. The COMMIT saves both the UPDATE inside the procedure and all pending modifications (including the inserts) in the current session transaction.",
          "is_correct": true
        },
        {
          "option": "C. The COMMIT is ignored because nested procedures are not allowed to control transactions.",
          "is_correct": false
        },
        {
          "option": "D. The procedure fails compilation because a COMMIT statement cannot exist within a subprogram.",
          "is_correct": false
        }
      ],
      "explanation": "Correct answer is B. A COMMIT statement commits the entire current transaction in the session, saving both the procedure's DML and any preceding DML from the calling block."
    },
    {
      "id": 20,
      "question": "Which of the following is true regarding functions in PL/SQL? [Select ALL correct answers]",
      "answers": [
        {
          "option": "A. A function can be called as part of an expression in a standard SQL SELECT query.",
          "is_correct": true
        },
        {
          "option": "B. A function can have NO parameters but must always declare a RETURN datatype.",
          "is_correct": true
        },
        {
          "option": "C. A function's return value must be a scalar database type; it cannot be customized.",
          "is_correct": false
        },
        {
          "option": "D. A function executes automatically in response to DML operations on a table.",
          "is_correct": false
        }
      ],
      "explanation": "Correct answers are A and B. Functions can be used in SQL syntax, and parameters are optional but a RETURN datatype is always required. C is wrong because functions can return complex types; D is wrong because triggers execute automatically in response to DML."
    },
    {
      "id": 21,
      "question": "How does the execution of a Database Trigger fundamentally differ from that of Procedures and Functions?",
      "answers": [
        {
          "option": "A. Triggers must be explicitly called with parameter values, whereas Procedures execute automatically.",
          "is_correct": false
        },
        {
          "option": "B. Triggers cannot perform DML statements on other tables, whereas Procedures can.",
          "is_correct": false
        },
        {
          "option": "C. Triggers execute (fire) automatically in response to specific events (like DML) on a table or view, requiring no application or explicit code call, whereas Procedures/Functions must be explicitly executed.",
          "is_correct": true
        },
        {
          "option": "D. Triggers are compiled at runtime on each execution, whereas Procedures and Functions are pre-compiled database objects.",
          "is_correct": false
        }
      ],
      "explanation": "Correct answer is C. Triggers fire automatically in response to events, while procedures and functions must be explicitly invoked."
    },
    {
      "id": 22,
      "question": "A database designer wants to write a trigger on the employees table that automatically capitalizes the employee's name (emp_name) before saving it to the table. Which configuration is appropriate and why?",
      "answers": [
        {
          "option": "A. An AFTER INSERT trigger, because name changes can only be verified after the record has been successfully inserted into the database.",
          "is_correct": false
        },
        {
          "option": "B. A BEFORE INSERT FOR EACH ROW trigger, because it allows the trigger to modify the :NEW.emp_name field directly before the data is written to the disk.",
          "is_correct": true
        },
        {
          "option": "C. A statement-level BEFORE INSERT trigger, because it optimizes performance by capitalizing all names in a single step.",
          "is_correct": false
        },
        {
          "option": "D. Either BEFORE or AFTER row-level trigger; they both allow modifications to the :NEW fields.",
          "is_correct": false
        }
      ],
      "explanation": "Correct answer is B. Only a BEFORE row-level trigger can modify :NEW fields; in AFTER triggers :NEW is read-only, and statement-level triggers do not have access to individual row :NEW/:OLD values."
    },
    {
      "id": 23,
      "question": "An employees table currently has 100 employee records. A developer executes the statement: UPDATE employees SET salary = salary * 1.05;. If there are two triggers active on the table: Trigger 1: CREATE TRIGGER tr1 AFTER UPDATE ON employees ... (Statement-level); Trigger 2: CREATE TRIGGER tr2 AFTER UPDATE ON employees FOR EACH ROW ... (Row-level). How many times will each trigger execute during this update transaction?",
      "answers": [
        {
          "option": "A. Trigger 1 will execute 1 time; Trigger 2 will execute 1 time.",
          "is_correct": false
        },
        {
          "option": "B. Trigger 1 will execute 100 times; Trigger 2 will execute 100 times.",
          "is_correct": false
        },
        {
          "option": "C. Trigger 1 will execute 1 time; Trigger 2 will execute 100 times.",
          "is_correct": true
        },
        {
          "option": "D. Trigger 1 will execute 0 times because no row filters were specified; Trigger 2 will execute 100 times.",
          "is_correct": false
        }
      ],
      "explanation": "Correct answer is C. A statement-level trigger (without FOR EACH ROW) executes once per DML statement, while a row-level trigger (FOR EACH ROW) executes once per affected row (100 times)."
    },
    {
      "id": 24,
      "question": "In a row-level database trigger, which of the following accurately describes the availability of the :OLD and :NEW qualifiers? [Select ALL correct answers]",
      "answers": [
        {
          "option": "A. During an INSERT statement, :OLD values are fully populated with default database parameters.",
          "is_correct": false
        },
        {
          "option": "B. During an INSERT statement, :OLD is NULL (or unpopulated), and :NEW contains the values to be inserted.",
          "is_correct": true
        },
        {
          "option": "C. During a DELETE statement, :OLD contains the existing row values being deleted, and :NEW is NULL (or unpopulated).",
          "is_correct": true
        },
        {
          "option": "D. During an UPDATE statement, :OLD contains the pre-update row values, and :NEW contains the post-update values.",
          "is_correct": true
        }
      ],
      "explanation": "Correct answers are B, C and D. :OLD is empty during INSERT, :NEW is empty during DELETE, and during UPDATE :OLD holds pre-update values while :NEW holds the proposed new values. A is wrong because :OLD is simply not populated during an INSERT."
    },
    {
      "id": 25,
      "question": "A junior developer attempts to compile the following trigger and gets an error: CREATE OR REPLACE TRIGGER trg_salary_cap AFTER UPDATE ON employees FOR EACH ROW BEGIN IF :NEW.salary > 200000 THEN :NEW.salary := 200000; END IF; END; What is the exact reason for the compilation error?",
      "answers": [
        {
          "option": "A. The database does not allow comparing :NEW values inside an IF statement.",
          "is_correct": false
        },
        {
          "option": "B. :NEW values are read-only in AFTER triggers and cannot be assigned new values.",
          "is_correct": true
        },
        {
          "option": "C. The parameter 200000 is missing a currency symbol or unit of measurement.",
          "is_correct": false
        },
        {
          "option": "D. FOR EACH ROW cannot be used in conjunction with AFTER triggers.",
          "is_correct": false
        }
      ],
      "explanation": "Correct answer is B. In AFTER triggers the DML changes have already been written to the table, so :NEW fields are strictly read-only; a BEFORE trigger must be used to modify proposed values."
    },
    {
      "id": 26,
      "question": "A developer wants to restrict a row-level trigger to fire only when the new salary is greater than 100,000. Which of the following syntaxes is valid for the trigger header?",
      "answers": [
        {
          "option": "A. CREATE TRIGGER trg ON employees FOR EACH ROW WHEN (:NEW.salary > 100000)",
          "is_correct": false
        },
        {
          "option": "B. CREATE TRIGGER trg ON employees FOR EACH ROW WHEN (NEW.salary > 100000)",
          "is_correct": false
        },
        {
          "option": "C. CREATE TRIGGER trg ON employees FOR EACH ROW BEGIN IF :NEW.salary > 100000 THEN ...",
          "is_correct": false
        },
        {
          "option": "D. Both B and C are syntactically valid methods of applying conditional criteria.",
          "is_correct": true
        }
      ],
      "explanation": "Correct answer is D. In the WHEN clause of a trigger header the colon prefix must NOT be used (option B is correct), and the check can alternatively be written with an IF statement inside the body using the colon prefix (option C)."
    },
    {
      "id": 27,
      "question": "Why is a database trigger prohibited from executing transaction control statements like COMMIT; or ROLLBACK; directly in its body?",
      "answers": [
        {
          "option": "A. Triggers run in a completely offline sandbox and do not interact with active transactions.",
          "is_correct": false
        },
        {
          "option": "B. Triggers execute as an integral part of the triggering DML statement's transaction. Allowing a trigger to commit would prematurely finalize parent transactions, breaking database integrity.",
          "is_correct": true
        },
        {
          "option": "C. Trigger execution blocks are too small to support the memory required for commits.",
          "is_correct": false
        },
        {
          "option": "D. Standard Oracle SQL syntax does not recognize the keyword COMMIT inside triggers.",
          "is_correct": false
        }
      ],
      "explanation": "Correct answer is B. A trigger operates as part of the transaction that executes the triggering DML statement; allowing COMMIT or ROLLBACK would prematurely finalize or roll back the parent transaction, compromising integrity."
    },
    {
      "id": 28,
      "question": "Trace the slide example trigger trg_audit_salary_update: CREATE OR REPLACE TRIGGER trg_audit_salary_update AFTER UPDATE OF salary ON employees FOR EACH ROW BEGIN IF :OLD.salary < :NEW.salary THEN INSERT INTO salary_audit VALUES (:OLD.emp_id, :OLD.salary, :NEW.salary, SYSTIMESTAMP); END IF; END; If Alice Smith has an initial salary of 5000.00 and an update is issued: UPDATE employees SET salary = 4800.00 WHERE emp_id = 101; What will be recorded in the salary_audit table?",
      "answers": [
        {
          "option": "A. A record showing a change from 5000.00 to 4800.00 with the timestamp.",
          "is_correct": false
        },
        {
          "option": "B. Nothing is inserted because the condition :OLD.salary < :NEW.salary evaluates to FALSE (4800 is not greater than 5000).",
          "is_correct": true
        },
        {
          "option": "C. The trigger throws a runtime error because salary decreases are not allowed.",
          "is_correct": false
        },
        {
          "option": "D. A row is inserted, but with NULL values for the salaries.",
          "is_correct": false
        }
      ],
      "explanation": "Correct answer is B. Since the old salary (5000) is greater than the new salary (4800), the condition :OLD.salary < :NEW.salary is FALSE, so the INSERT is bypassed and nothing is recorded."
    },
    {
      "id": 29,
      "question": "In the activity trg_prevent_salary_decrease, the trigger must prevent the salary from decreasing and prevent the salary from exceeding 200,000. To successfully prevent these updates from being written to the database, what should the trigger do?",
      "answers": [
        {
          "option": "A. Execute a ROLLBACK; statement directly inside the exception handler.",
          "is_correct": false
        },
        {
          "option": "B. Raise a custom exception or use RAISE_APPLICATION_ERROR to interrupt execution, which halts the DML statement and rolls back its changes.",
          "is_correct": true
        },
        {
          "option": "C. Reset the :NEW.salary := :OLD.salary; silently without notifying the user application.",
          "is_correct": false
        },
        {
          "option": "D. Delete the modified employee record entirely from the table.",
          "is_correct": false
        }
      ],
      "explanation": "Correct answer is B. Raising a runtime error (e.g., RAISE_APPLICATION_ERROR) halts execution, aborts the SQL statement, and rolls back its changes while notifying the application. Direct ROLLBACK is illegal in triggers."
    },
    {
      "id": 30,
      "question": "An update statement on employees fires a BEFORE UPDATE trigger, which updates a record in the departments table. The update in departments subsequently fires a trigger on departments which inserts a row in audit_log. Which of the following is true?",
      "answers": [
        {
          "option": "A. The execution of nested triggers is illegal; the engine will crash on the second trigger.",
          "is_correct": false
        },
        {
          "option": "B. All trigger executions are flattened, and they execute concurrently in parallel threads.",
          "is_correct": false
        },
        {
          "option": "C. The triggers execute sequentially in a cascading fashion within the single, shared transaction boundary. If any trigger fails, all changes in the cascade are rolled back.",
          "is_correct": true
        },
        {
          "option": "D. The parent update must commit before the department trigger can fire.",
          "is_correct": false
        }
      ],
      "explanation": "Correct answer is C. Oracle supports cascading triggers within the same session and transaction boundary; if any trigger fails, the entire chain is rolled back."
    }
  ]
}
